name: 'Asana Integration'
description: 'Manage Asana tickets via Github Actions'
inputs:
  access-token:
    description: 'Asana Personal Access Token'
    required: true
  comment:
      description: 'Asana comment text'
      required: true
  link:
    description: 'Link to be posted'
    required: true
runs:
  using: 'node12'
  main: 'index.js'
